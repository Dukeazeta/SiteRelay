{
  "name": "@siterelay/browser-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit --pretty false",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@siterelay/capture-schema": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "esbuild": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}

