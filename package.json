{
  "name": "siterelay",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev:service": "pnpm --filter @siterelay/capture-service dev",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
